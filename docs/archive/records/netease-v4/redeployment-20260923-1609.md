# V4 当前代码重新编译、开发部署与清理（2026-09-23）

> 用户指定：编译并部署到 `D:\data\avalonia\Controls`，不部署 libVLC。
> 源码提交：`235ea4071e5b830ad5818cfa31192901b581cd4b`。V5 仍为方案，本次部署的是当前 V4 功能代码。
> [部署文件摘要](assets/redeployment-20260923-1609.json) · [部署维护说明](../../../maintenance/deployment-and-release.md)。
> 部署完成时间：北京时间 16:11；随后根据用户要求清理中间文件。

## 构建与部署结果

- 对 Plugin 执行 locked restore，以 Debug、警告视为错误、`IncludeLibVlcRuntime=false` 编译，结果 0 警告、0 错误。
- 使用独立输出 `artifacts/development-deployment-20260923-160902/bin/`，通过 `DeployManagedPlugin` 先生成干净暂存目录，再部署到 `D:\data\avalonia\Controls\MusicNetEasePlugin`。
- 部署前确认 Host/Standalone 未运行，目标文件可独占读取，目标路径及其内容无重解析点，Controls 内只有一份匹配的插件身份。
- 原插件完整备份至 `artifacts/development-deployment-20260923-160902/previous-plugin/`，备份的文件数量及 SHA256 与原目录一致；备份位于仓库内，不进入 Controls 扫描范围。
- 实际部署共 12 个文件，每个文件的名称、长度和 SHA256 均与干净暂存产物一致。manifest 由构建生成，插件版本保持 `1.0.0`。

本次产物不含原生 `native` 目录、`libvlc.dll` 或 `libvlccore.dll`，不复制公共运行库；保留必需的托管桥接库 `LibVLCSharp.dll`。不携带 Host 共享的 Avalonia、Dock 或 PluginSdk 程序集。

入口程序集 `MusicNetEasePlugin.Plugin.dll` 的 SHA256：

```text
B057397A821ABAAE62CDD3A495F580A2BC35A2EDAC932264DB4BEE4DFB6EAAD8
```

## 保留项与核对

| 范围 | 部署前后结果 |
| --- | --- |
| `D:\data\avalonia\Common` | 425 个文件的相对路径、长度和 SHA256 全部一致 |
| 插件本机用户数据 | 4 个文件的相对路径、长度和 SHA256 全部一致；未改写账号或播放设置 |
| 其他插件 | 12 个插件目录的名称及修改时间一致 |
| 当前仓库生产代码 | 本轮无修改 |

完整构建日志、暂存产物、备份和本地核对数据保留在 `artifacts/development-deployment-20260923-160902/`。归档摘要仅保留部署文件信息与其他范围的核对计数，不收录用户数据内容。

## 验证边界

本轮执行依赖恢复、无原生库构建、干净部署与摘要验证。沿用同一会话前一轮 V4 本地开发检查：276 项测试、0 失败、0 跳过，166 项门禁自测通过，运行编号 `20260923-074711-28e5c289`；之后生产代码未变，因此不重复运行完整业务测试。

本轮没有启动 Host，文件部署成功不等于本次真实声卡、Dock 或重启恢复已验收。此前 V1–V4 的用户验收结论独立保留。未使用 AIFLOW、Windows CI、正式 ZIP 或发布门禁，未推送远端；未清理本轮回滚备份。

## 中间文件清理

用户随后要求清理中间文件。清理前逐项核对本仓库四个项目的 `bin/obj`、本轮及历史构建输出/部署暂存、四个旧提交消息或指针文件；范围不含实际 Controls、Common、用户数据、回滚副本、测试证据和源码。清理计划包含完整绝对路径、文件数量及大小，检查了版本控制文件和重解析点。

批量递归删除被自动审批审核拒绝，工具仅返回 `blocked by policy`，未给出更具体原因；该命令未执行。没有换工具或拆分相同删除操作重试。随后使用构建系统维护的产物清单执行标准清理：

```powershell
dotnet clean MusicNetEasePlugin.slnx -c Debug -v:minimal
dotnet clean src/MusicNetEasePlugin.Plugin/MusicNetEasePlugin.Plugin.csproj -c Debug -v:minimal `
  -p:IncludeLibVlcRuntime=false `
  -p:OutputPath=D:\code\local\avalonia_dock_plug_test\myavalonia-music-netease\artifacts\development-deployment-20260923-160902\bin\
```

两次标准 clean 均成功，删除 **4,130 个文件、2,054,898,130 字节（约 1.91 GiB）**。仍有 **551 个文件、约 137.05 MiB** 的历史暂存、NuGet 还原缓存和小型临时文件；逐项绝对路径及大小见[清理结果](assets/redeployment-cleanup-20260923-1609.json)。其中最大的剩余项是本仓 `artifacts/m1-development-20260923/builtin/Controls`，约 106.57 MiB，它不是实际部署的 Controls。

清理后，实际部署的 12 个文件和 Common 的 425 个文件再次核对摘要一致，旧插件备份保留。部署完成时用户数据的 4 个文件摘要一致；清理后的第二次完整比较遇到被其他进程占用的运行缓存 `.owner` 文件，故记为未完成，不宣称用户数据在运行期间仍完全不变。本次清理范围未包含用户数据。

本轮清理结果作为此前清理记录的后续状态保留，不改写旧记录。未达到“全部中间文件清空”，剩余项受上述自动审批限制；如手工清理，只使用 JSON 中的精确路径，保留 `previous-plugin`、日志及核对记录。下次构建先执行 locked restore，再执行 build。
