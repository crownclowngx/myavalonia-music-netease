# 网易云音乐 V2：M1 搜索与单曲播放实施方案

> 状态：方案已编写，业务代码、播放探针及 M1 开发门禁扩展尚未实施。
> 日期：2026-09-22。源码基线：`c469f919e931e60d30797cbd357d245e84e7cf01`，工作区另含前两轮尚未提交的路线图和文档整理。
> 编号：承接已归档的 [V1 登录方案](../archive/plans/netease-v1-flurl-login-plan.md)，使用 **V2**；对应能力路线图的 **M1**，不改变插件包版本或 SDK 版本。
> 配套：[V2 / M1 专用开发验证矩阵](../maintenance/netease-v2-m1-playback-verification.md) · [能力路线图](netease-capability-roadmap.md) · [当前登录契约](../reference/netease-http-session.md)。

## 1. 目标与首要规定

**目标：用户在已登录的插件里搜索歌曲，从结果中选中一首，实际播放，并能暂停、继续、停止、调节音量和查看进度。** 登录沿用现有成果。本方案将 M1 拆成可以实现和验证的步骤；本文中的新增类型、脚本参数和文档路径均是计划，不表示已经存在。

以下要求贯穿整个 V2 实施过程：

1. **SOLID 是首要规定。** 按变化原因分开账号会话、音乐 API、媒体取流、音频引擎、播放编排与界面投影；依赖方向、资源所有者和失败责任必须清楚。不得以赶通播放为由把网络、协议和音频控制堆进 ViewModel。
2. **设计模式朴素使用。** 使用普通类、不可变记录、枚举、构造注入和少量窄接口。仅在需要隔离网络、系统音频、文件、时间或 UI 调度的边界引入替换点，不引入通用播放器框架、事件总线、插件化协议平台或状态机库。
3. **注释使用详细中文。** 说明设计思路、协议依据、线程与取消约定、数据和资源寿命、失败后的状态；公开契约写明输入、输出、异常和调用限制，不逐行翻译代码。
4. **单元测试与本地开发门禁齐全。** 专用矩阵覆盖正常路径、失败、竞态、资源释放及登录回归；门禁校验实际测试结果，不只看命令退出码。真实音频输出与 Host 联调单独留证。
5. **实现与文档同步。** 本方案维护阶段进度，专用矩阵维护测试映射；实际协议、操作步骤和当次验证证据各归其位，不把规划写成现行能力。
6. **不使用 AIFLOW，不新增或运行 Windows CI、发布门禁。** 不执行发布 Smoke、seal、发布覆盖率、发布重复性检查或正式 ZIP 发布。Windows 本机 Debug 验证和真实 Host 开发联调属于本阶段；发布时再使用发布门禁。
7. **保留既有技术边界。** 插件主动 HTTP 统一使用 Flurl；Plugin 是唯一业务和界面实现，Standalone 与 Tests 复用它；通过公开 SDK 接入 Host，当前目标为 Windows x64。

## 2. M1 范围与完成条件

| 能力 | V2 交付范围 | 本阶段行为 |
| --- | --- | --- |
| 歌曲搜索 | 歌名/歌手关键词、歌曲类型、分页、加载、空结果、失败重试 | 已登录后使用；提交按钮或回车搜索，默认每页 30 条；新搜索替换旧结果 |
| 歌曲详情 | ID、名称、歌手、专辑、封面、时长 | 搜索摘要可先展示；按选中歌曲补取详情，封面失败使用占位图 |
| 播放资源 | 当前账号的原始网易播放地址及必要格式、音质、试听信息 | 首版请求 `standard`，不提供音质选择；实际可播性由当次返回决定 |
| 单曲播放 | 加载、播放、暂停、继续、停止、自然结束、音量 | 一个插件容器最多一个活动音源；选另一首会替换当前单曲，不形成队列 |
| 播放信息 | 当前歌曲、当前时间、可播放时长、状态和失败提示 | 进度只读；试听明确标记，整曲元数据时长不冒充实际可播时长 |
| 页面 | 账号区、歌曲搜索结果区、单曲播放控制区 | 保留微信/App 登录、恢复、重试保存与退出；按钮有效性与状态一致 |
| 生命周期 | 切歌、停止、页面关闭、退出账号、Host 停止 | 取消请求、拒绝迟到结果、停止音源并释放资源，规则见第 6 节 |

歌单、队列、上一首/下一首、循环/随机、进度拖动、歌词、喜欢、下载任务、最近播放持久化、系统媒体控制及跨页面后台连续播放归后续里程碑。V2 不自动上报听歌记录，不增加跨插件工作台命令或 Workflow Action。

完成 M1 必须同时满足：

- 一首真实账号可播歌曲完成“搜索 → 选中 → 资源解析 → 实际出声”，暂停、继续、停止和音量有效。
- 快速切换 A/B 时仅最新选择生效，无双重播放；停止或退出后旧响应不能再次启动音频。
- 无地址、试听、格式不支持、设备不可用和网络失败都有明确结果，单曲失败不误判整个账号失效。
- 新增自动测试和既有登录回归通过；Standalone 与真实 Host 的音频和关闭行为分别验证。
- 当前契约、快速开始、专用矩阵、导航与当次实施记录同步更新，未覆盖事项明确保留。

## 3. 现状与关键前置验证

| 已核对事实 | 对 M1 的影响 |
| --- | --- |
| `LoginCoordinator` 私有持有已提交的 `AuthContext`，对外主要提供 `LoginSnapshot` | 需要受控会话访问，不能让音乐 API 反复从磁盘读 Cookie 或让 UI 持有凭据 |
| `NeteaseProtocol` 仅有 Eapi、Weapi；`NeteaseTransport` 使用登录异常类型 | 新增 xeapi，并把通用传输错误与登录/音乐端点语义分开 |
| 当前业务响应预算为 15 秒、1 MiB；头像有单独限制 | 媒体数据需要独立取流与缓冲，不能用 JSON 响应读取器加载音频 |
| Plugin 尚无音频依赖，Lifecycle 只收口登录 | 必须验证引擎、解码与 Host 依赖加载，扩展停止和释放顺序 |
| 已有 xUnit、Flurl 测试、Headless、可控时间与开发检查脚本 | 在现有测试项目及检查入口上扩展，保留登录测试，不复制第二套门禁 |

### 3.1 本轮接口选定范围

固定上游继续使用 `a8c781fd64faab17fedfd46e0615a2609307f163`。下表中的 `/api/…` 是网易逻辑端点，由协议适配生成最终路径，不把 Enhanced 外壳路由直接发给网易。

| 用途 | 上游模块 / 原生逻辑端点 | 方案 |
| --- | --- | --- |
| 搜索歌曲 | `cloudsearch` / `/api/cloudsearch/pc` | 采用 `type=1`、关键词、limit/offset；固定配置下默认 eapi，按请求核心一起核对。[模块](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/module/cloudsearch.js)、[配置](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/util/config.json) |
| 歌曲详情 | `song_detail` / `/api/v3/song/detail` | weapi；按需补取选中歌曲，保留长整数 ID，不照搬 JS 数值精度限制。[模块](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/module/song_detail.js) |
| 播放地址 | `song_url_v1` / `/api/song/enhance/player/url/v1` | xeapi；请求 standard，解析实际返回，省略独立音源匹配分支。[模块](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/module/song_url_v1.js) |
| xeapi 初始化 | `register_xeapikey` 及初始化逻辑 | 核对公钥获取、格式、有效状态与必要初始化；只移植当前已登录播放链需要的部分。[公钥模块](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/module/register_xeapikey.js)、[初始化](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/generateConfig.js) |

xeapi 必须按固定[加密实现](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/util/crypto.js)和[请求核心](https://raw.githubusercontent.com/NeteaseCloudMusicApiEnhanced/api-enhanced/a8c781fd64faab17fedfd46e0615a2609307f163/util/request.js)整体对照：请求封装、公钥状态、响应解密和协议会话均纳入验证。C# 中的协议会话按插件容器及账号代次隔离，不照搬上游模块级可变状态；本版先不持久化协议会话密钥。

### 3.2 音频引擎与媒体输入决策

优先验证 **NAudio 的本地媒体读取与 Windows 音频输出适配**。其官方资料提供 Windows 输出和解码能力说明，但不代表当前插件的目标框架、私有依赖及真实返回格式已经兼容；具体包、稳定版本和输出实现由 S0 验证后锁定。[NAudio 官方项目](https://github.com/naudio/NAudio)

**M1 首选有界临时文件方案：Flurl 下载当前单曲 → 完成校验 → 引擎读取本地文件 → 系统输出。** 这样能保持 HTTP 所有权统一，并先把取消、文件和解码寿命做完整。等待下载期间显示“正在加载”，不承诺边下边播；该延迟取舍须在真实歌曲样本中观察。

S0 必须给出：所选包和框架兼容性、至少一种真实返回格式的可用解码链、暂停/继续/停止/音量结果、是否引入原生资产、Host 加载结果、加载耗时与支持范围。MP3/WAV 受控样本成功不能外推所有网易音频都可播。依赖版本以锁定包为准，不照抄某一历史大版本的 API 名称。

如果首选引擎或输入方式不满足实际格式与 Host 加载要求，先记录原因，再替换该适配边界；不通过引擎自行联网绕过 Flurl 约定。`song_url` 仅保留为有证据的协议替代候选，未经实测不静默降级或将“搜索可用”标记为 M1 完成。

## 4. SOLID 分工与设计思路

以下名称为拟定名称，实现时可按仓库风格微调，但职责和依赖方向应保持。

| 组件 | 唯一主要职责 | 设计理由与所有者 |
| --- | --- | --- |
| 已提交账号会话对象 / `IMusicSessionAccessor` | 提供受控请求快照、撤销信号与同代次响应提交 | 登录编排和音乐请求共用一份已提交会话；容器拥有，UI 不读取它 |
| `IMusicCatalogApi` | 搜索、详情的类型化请求与结果转换 | 与播放引擎无关，不增加到 `INeteaseAuthApi` 中 |
| `IPlaybackResourceResolver` | 为歌曲解析播放资源和不可播/试听结果 | 播放资源协议可独立变化，不要求搜索接口知道媒体流 |
| `NeteaseTransport` 与协议编码器 | Flurl 请求、有限读取、公共失败信息与加解密 | 复用并渐进调整；端点业务含义留给相应 API |
| `IMediaBuffer` | Flurl 媒体获取、有界临时文件及清理 | 不启动音频；返回具备明确释放责任的媒体资源 |
| `IAudioOutput` 及具体适配 | 本地媒体打开、输出、暂停、继续、停止、音量与进度事件 | 音频库类型留在 Infrastructure；与 Cookie、网易 API、Avalonia 分离 |
| `PlaybackCoordinator` | 单曲播放状态、操作顺序、切歌代次、错误和资源所有权转移 | 容器内一个实例，使用普通方法和枚举；不自己实现 HTTP 或解码 |
| 搜索页面模型、播放区模型 | 局部命令、结果展示、订阅与 UI 调度 | 每个 Document 拥有；只消费应用结果，不直接触碰磁盘或音频库 |
| `MainDocument` 与插件 Lifecycle | 页面组合、Document 所有者信号、Host 启停 | MainDocument 保持轻薄；Lifecycle 明确先停止任务、再释放依赖 |

SOLID 的审查要求：

- **单一职责：** 修改搜索协议无需改音频输出，替换引擎无需改登录 UI；实际代码据此分组。
- **开闭原则：** 不为未来 440 个接口预建框架；新增需要只扩展相应窄适配，稳定用例无需识别具体实现类型。
- **里氏替换：** 真引擎与测试替身遵守同一状态、取消、异常、事件和释放契约，不能靠假实现省略困难路径。
- **接口隔离：** 搜索只依赖目录查询，播放器只依赖资源、媒体与音频；只需两个操作的调用方不接收“万能网易服务”。
- **依赖倒置：** Application 定义需求，Infrastructure 实现端口，唯一服务注册入口组装；不从业务类解析 DI 容器。

建议沿用现有目录增加 `Application/Music`、`Application/Playback`、`Infrastructure/Audio` 与 `Infrastructure/Media`。网络适配继续在 `Infrastructure/Http`，页面在 `Features`。不额外拆公共 NuGet 包或建立第二套 Standalone 实现。

## 5. 会话、错误与数据约定

### 5.1 已登录会话访问

1. 登录候选会话仍由登录尝试拥有；只有账号核验并提交后，音乐业务才能获得不可变请求快照。保存失败但内存登录有效的账号仍可播放。
2. 请求携带账号代次、凭据版本和取消信号。账号代次在退出/新会话时变更，同一账号重新登录也视为新代次；不复用登录 UI 的 `Revision` 作为账号身份。
3. 会话失效或退出立即撤销音乐请求资格，清除搜索/播放投影并停止输出；网络取消不能取消本地清理责任。
4. 迟到响应只有在同账号代次、适用凭据版本时才能提交 Cookie/协议状态。冲突响应不覆盖新凭据；必要的受保护保存由会话所有者统一负责，沿用保存失败反馈。
5. 网络、磁盘和音频 await 不持有状态锁；取消回调、事件通知和跨组件调用放在锁外。调整登录提交逻辑时完整回归原有保存、取消补偿与退出行为。

### 5.2 结果与错误

歌曲模型只承载业务展示数据；资源模型仅由播放链持有临时 URL、格式、实际音质、有效信息和试听范围，不进入日志、ViewModel 或持久化队列。

| 分类 | 用户行为 |
| --- | --- |
| 搜索空结果 | 显示空状态，不作为网络错误 |
| 无播放地址 / 明确受限 | 显示暂不可播放或服务端有依据的具体原因，保留账号状态 |
| 试听 | 显示试听标记并按实际范围播放，不把试听结束记为播放故障 |
| 登录确实失效 | 撤销对应账号代次的音乐操作，提示重新登录；不因媒体 CDN 的 403 直接退出账号 |
| 限流 / 额外验证 | 停止本次自动流程，保留可操作提示，不高速重试或自动重登 |
| 网络、超时、无设备、解码失败 | 释放本次资源，显示可重试失败；不伪装为正在播放 |
| 用户取消 / 切换 / 正常停止 | 正常收口，不弹出失败提示 |

音乐 API 失败使用独立业务错误分类；公共传输层保留必要 HTTP 状态、业务码、Retry-After 和失败阶段，不向上泄露原始正文、Cookie、协议密钥或完整资源 URL。既有登录适配继续提供原有 `AuthException` 契约。

### 5.3 请求与临时媒体策略

下面是 **M1 建议初始配置**，不是网易限制或既有实现；S0 用实际样本校准后记录最终值。

| 项目 | 初始策略 |
| --- | --- |
| 搜索与详情 | 单次 15 秒、JSON 上限 1 MiB；一页 30 条，不无限累计全部结果 |
| 媒体请求 | 独立 Client，无账号 Cookie/CSRF；只接受经校验的来源与协议，重定向逐跳核对且最多 3 次 |
| 媒体读取 | 响应头和读取空闲预算各 15 秒，总预算 120 秒；实际已读字节上限 64 MiB，不能只信 Content-Length |
| 临时文件 | 插件数据目录下按实例/操作生成唯一名称；校验完成前为未完成文件，引擎不读取半成品 |
| 失败重试 | 普通网络失败由用户重试；明确为地址过期且尚未输出时，最多重新解析 1 次，不对所有 403 盲目重试 |
| 正常清理 | 替换、停止、自然结束、失败、页面所有者关闭、账号退出和 Host 停止时，先释放引擎/句柄，再删除本次文件 |
| 异常残留 | 使用实例占用标识，后续启动只清理可确认无活动所有者的插件自有残留；路径必须在专属目录内，不清扫通用 Temp 或其他实例 |

不按 URL 扩展名或 Content-Type 单独认定格式；检查实际媒体和解码结果，拒绝错误页、截断数据和异常大小。文件删除失败须可诊断并留待受控清理，不能在持有文件句柄时反复删除。M1 不承诺断点续传、离线曲库或保留下载文件。

## 6. 播放状态、竞态与页面行为

| 当前状态 | 可用操作 | 结果 |
| --- | --- | --- |
| Idle / Stopped / Ended / Failed | 从结果选择播放；可重试当前歌曲 | 创建新操作，重新取得有效资源 |
| Loading | 停止；改选另一首 | 取消旧解析/下载/打开，清理后只保留最新操作；暂停不可用 |
| Playing | 暂停、停止、调音量、改选歌曲 | 暂停保留当前媒体位置，停止释放本次媒体 |
| Paused | 继续、停止、调音量、改选歌曲 | 继续当前媒体；停止后不承诺可直接复用旧 URL |
| 音频自然结束 | 再次播放或选择别的歌曲 | 进入 Ended 并释放媒体，不自动播放下一首 |

“Playing”由引擎确认输出运行后发布，调用 Play 方法成功返回不自动等同用户已听到声音。进度以引擎时间为准，通知节流且切回 UI 线程；音量 0 仍是播放状态，不能用静音推断引擎失败。

每次选歌登记新的播放操作代次，取消并等待旧操作完成清理后再接管引擎。解析、下载、音频事件和 UI 投影均检查代次；快速 A → B → C 只允许 C 最终接管，已失效的 B 不继续排队出声。停止等控制操作也按顺序处理，避免暂停完成回调覆盖更新后的停止状态。

**M1 页面关闭约定：** 播放尝试与音频归属最近一次发起播放的 Document。该 Document 真正关闭时停止其播放；其他页面关闭不能停止它。临时移出视觉树、切换 Dock 可见性不等于 Document 关闭。多页面共享一个播放器状态；不创建各自的音频引擎。跨页面关闭后继续后台听歌留给 M2。

退出账号先撤销资格、取消音乐工作并立即请求停止音频，再收口会话清理；即使远端退出失败，也不继续播放。Host 停止统一撤销入口，等待搜索、播放和登录工作收口，再释放媒体、引擎及 Flurl Client。同步 Dispose 路径不得等待必须回到 UI 线程的 continuation。

页面布局维持一个 Document：账号区展示连接状态，主体为搜索框及分页结果，播放区展示单曲和控制。加载与错误按区域呈现；保留编译绑定、合理的最小窗口布局和占位图，不暴露 xeapi、临时路径等实现细节。

## 7. 中文注释要求

| 位置 | 必须说明的内容 |
| --- | --- |
| 接口和公开方法 | 职责、参数单位、返回所有权、可用状态、取消与失败约定，调用方是否需要 Dispose |
| 会话与播放协调器 | 为什么区分账号代次/凭据版本/播放代次；锁保护什么；在哪一步转移资源所有权 |
| xeapi 编码与响应 | 固定来源、序列化/编码差异、公钥与协议会话寿命、解密边界，哪些值仅用于测试固定输入 |
| 音频适配 | 引擎线程、初始化限制、事件意义、暂停/停止差异、文件句柄释放顺序与设备失败处理 |
| 媒体临时文件 | 大小与超时理由、半成品处理、取消和异常残留清理；为什么不属于用户下载 |
| 关键测试 | 要复现的真实失败、同步屏障放置原因、关键断言保护的行为 |

示例注释应解释原因，例如：“先使旧播放代次失效，再取消下载；否则取消返回前到达的地址仍可能启动旧歌曲。”不使用大段模板注释替代准确契约，也不在注释中记录真实票据。

## 8. 实施步骤与退出条件

| 阶段 | 实施内容 | 退出条件 / 专用矩阵 |
| --- | --- | --- |
| S0：协议与引擎最小验证 | 核对三个音乐端点及 xeapi 依赖；固定对照样本；候选引擎读取受控文件与真实资源；验证 Host 依赖 | 有可复现的协议、格式、框架和加载结论，锁定实现路径；P、E、R 类证据分别记录 |
| S1：会话与基础传输 | 最小会话访问、账号撤销、协议会话隔离、错误分类、xeapi 支持 | P、A 类通过，既有登录回归无退化 |
| S2：搜索与详情 | 歌曲模型、cloudsearch 分页、详情补取、图片及失败状态 | C、U 类相关用例通过，新旧搜索结果不交叉 |
| S3：单曲播放用例 | 媒体缓冲、音频适配、播放状态、代次、清理与试听 | B、L、E 类通过，无旧音源复活或资源泄漏 |
| S4：页面与 Host 集成 | 页面组合、命令、状态展示、Document 所有权、生命周期 | U 类通过；Standalone 与 Host 实际输出及关闭观察完成 |
| S5：开发收口 | 门禁映射/自测、全量回归、中文注释审查、同步文档和当次记录 | G、D、R 类满足；M1 全部完成条件有证据，剩余限制准确列出 |

S0 可以先使用已知歌曲 ID，S2 搜索开发可在接口边界明确后进行；完整音乐页面接入前必须确认实际播放路径。技术验证出现失败时修复或更换对应适配，不将未完成环节用替身结果替代。

## 9. 单元测试与本地开发门禁

专用用例、拟建测试文件、自动/人工边界及门禁自测见[V2 / M1 验证矩阵](../maintenance/netease-v2-m1-playback-verification.md)。继续使用现有 Tests 项目，新增测试与所有登录回归在同轮运行。

计划扩展已有 `tools/verify-development.ps1`，增加 `-Milestone M1`：复用同一 restore/build/test/报告校验实现，增加 M1 场景映射检查和独立结果目录。当前脚本没有这个参数，以下命令在 S5 实现后才可使用：

```powershell
pwsh -NoProfile -File tools/verify-development.ps1 -Milestone M1
```

无参数入口保持兼容。门禁必须包括脚本自测、锁定还原、Debug 零警告构建、全量测试、实际 M1 用例覆盖校验、TRX 完整性、文档链接/锚点及 Git 差异格式检查。每轮使用独立目录，缺结果、零测试、跳过必测项或残缺报告均失败。

默认自动测试不访问网易、不读取真实凭据、不要求声卡出声；真实解码可用受控本地样本验证，真实输出与 Host 操作列入单独的本地开发验收。门禁输出分别记录自动、真实账号、音频输出、Host、部署与发布活动，不用一项通过代替其他项。

不设任意开发覆盖率百分比或发布包重复性指标。测试通过后，只有后续代码变化、新失败或具体未解问题才补跑相关检查。

## 10. 文档与依赖同步

| 位置 | 维护内容 |
| --- | --- |
| 本方案 | S0–S5 进度、技术决策、M1 边界和未完成事项 |
| [专用开发验证矩阵](../maintenance/netease-v2-m1-playback-verification.md) | P/A/C/B/L/E/U/D/G/R 场景映射到真实测试及证据，持续维护 |
| `docs/reference/netease-music-playback.md`（实施后新增） | 已接入搜索/详情/播放接口、音频格式、媒体限额、状态和生命周期现行契约 |
| `docs/quick-start/netease-playback.md`（实施后新增） | 实际可用的搜索、播放、停止、音量、试听及失败排错步骤 |
| `docs/archive/records/netease-v2/`（实施时新增） | 带日期的选型、实施、TRX、真实播放、Host 和开发部署记录，分别列出未执行项 |
| README、文档总导航、能力路线图、上游清单与登录契约 | 链接与状态同步；音乐能力完成后才将 M1 勾选完成 |
| 集中包版本、Plugin 依赖和 lock 文件 | 选型后同步版本、PackageReference、必要私有及传递运行时资产；按现有开发部署规则检查 |

保留上游及依赖所需的版权/许可说明，使用固定提交和锁定包。新增包不得把 Host 共享 SDK/Avalonia 等混入插件私有目录；若候选包要求改变目标框架，S0 先验证 Plugin、Standalone、Tests 和 Host 的整体兼容性，不只验证一段控制台代码。

## 11. 进度与当前验证状态

- [x] 编写 V2 / M1 实施方案与专用开发验证矩阵，并接入现有文档导航。
- [ ] S0：协议、引擎、媒体输入和 Host 依赖最小验证。
- [ ] S1：会话与传输扩展。
- [ ] S2：搜索、分页与详情。
- [ ] S3：单曲播放、试听及资源清理。
- [ ] S4：生产界面、Standalone 和 Host 集成。
- [ ] S5：M1 本地门禁、真实验收与文档收口。

本次工作是文档编写与静态源码核对，并查阅了正文链接的上游源码和候选音频库官方资料。未执行音乐接口实测、音频引擎验证、业务单测、Host 联调、部署或发布；不能据此认为 M1 已实现。
